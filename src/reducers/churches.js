import churchesIndex from '../data'
const INITIAL_STATE = churchesIndex

const churchesReducer = (state=INITIAL_STATE, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default churchesReducer
